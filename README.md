# Candidate Repository – Car & Track Task

## Task Description
Implement `Car` and `Track` classes with simple fuel consumption logic, and cover them with unit tests.

### How to Run
```bash
pip install -r requirements.txt
pytest -v
python main.py
```
